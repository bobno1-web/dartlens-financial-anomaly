# OpenDART 산업대비 재무 이상징후 도구 (Ralph Loop 1~4)
# 분석 엔진
requests>=2.31
PyYAML>=6.0
python-dotenv>=1.0
openpyxl>=3.1
XlsxWriter>=3.1

# Web UI — Flask 재구축 (Loop 20-A). 기본 실행: python app_flask.py
Flask>=3.0
pandas>=2.0

# (이전 Streamlit UI는 Loop 20에서 Flask로 재구축되며 제거 — streamlit 의존성 불필요. 롤백은 git 이력.)

# 개발/테스트
pytest>=7.4
